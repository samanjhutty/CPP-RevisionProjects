#include "DefaultParameters.h"
#include <iostream>
using namespace std;

DefaultParameters::DefaultParameters()
{
    private bool uinput;
    cout<<"Press 1 to load default parameters else 0 to load custom: ";
    cin>>uinput;

    if(uinput)
    {
        //todo

    }
    else
    {
        //todo
    }
}

